diff --git a/mindone/diffusers/models/transformers/transformer_hidream_image.py b/mindone/diffusers/models/transformers/transformer_hidream_image.py
index abcdef1..1234567 100644
--- a/mindone/diffusers/models/transformers/transformer_hidream_image.py
+++ b/mindone/diffusers/models/transformers/transformer_hidream_image.py
@@ -90,7 +90,7 @@ class HiDreamImagePatchEmbed(nn.Cell):
         self.out_channels = out_channels
         self.proj = mint.nn.Linear(in_channels * patch_size * patch_size, out_channels, bias=True)
 
-    def construct(self, latent):
+    def construct(self, latent) -> ms.Tensor:
         latent = self.proj(latent)
         return latent
 
@@ -578,7 +578,7 @@ class HiDreamImageTransformerBlock(nn.Cell):
         encoder_hidden_states: Optional[ms.Tensor] = None,
         temb: Optional[ms.Tensor] = None,
         image_rotary_emb: ms.Tensor = None,
-    ) -> ms.Tensor:
+    ) -> Tuple[ms.Tensor, ms.Tensor]:
         wtype = hidden_states.dtype
         (
             shift_msa_i,
@@ -636,7 +636,7 @@ class HiDreamBlock(nn.Cell):
         encoder_hidden_states: Optional[ms.Tensor] = None,
         temb: Optional[ms.Tensor] = None,
         image_rotary_emb: ms.Tensor = None,
-    ) -> ms.Tensor:
+    ) -> Union[ms.Tensor, Tuple[ms.Tensor, ms.Tensor]]:
         return self.block(
             hidden_states=hidden_states,
             hidden_states_masks=hidden_states_masks,
@@ -826,5 +826,5 @@ class HiDreamImageTransformer2DModel(ModelMixin, ConfigMixin, PeftAdapterMixin, F
         attention_kwargs: Optional[Dict[str, Any]] = None,
         return_dict: bool = False,
         **kwargs,
-    ):
+    ) -> Union[Tuple[ms.Tensor], Transformer2DModelOutput]:
         encoder_hidden_states = kwargs.get("encoder_hidden_states", None)